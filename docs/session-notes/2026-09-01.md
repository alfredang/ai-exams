# Session checkpoint — 2026-09-01

## Repository status

- Repository: `https://github.com/alfredang/ai-exams`
- Production: `https://exams.tertiaryinfotech.com`
- Branch: `main`
- Commit `c7b1104` (`feat(search): improve catalog matching and guidance`) was
  pushed successfully to GitHub, together with course-code mapping commit
  `1f73222`.
- Local `main` and `origin/main` matched immediately after the push.
- Do not change or reset any existing user passwords.

## Housekeeping completed

- Reduced ESLint output from 129 warnings to zero.
- Removed unused imports, constants, and obsolete seed reference constants.
- Refactored older React state/effect patterns in sharing, bulk selection,
  checkout payment selection, and the SEO saved-state notification.
- Replaced applicable image elements with Next.js image handling.
- Resolved the Handlebars webpack warning in the email-template system by
  using its browser-safe CommonJS build.
- Added `prisma.config.ts` and removed the deprecated Prisma configuration
  from `package.json` in preparation for Prisma 7.

## Site improvements already included

- Standardized customer-facing prices as Singapore dollars with `S$`.
- Added and linked the Contact/Support page.
- Corrected support email links to use `sales@tertiarycourses.com.sg`.
- Improved search support, accessibility labels, structured data, contact
  details, and delivery/refund information.
- Updated remaining ExamNova references to Tertiary Exams branding where
  appropriate.

## Verification completed

- Type checking passed.
- ESLint passed with zero warnings.
- Prisma validation passed.
- Production build passed without the previous Handlebars or Prisma warning.
- Local link crawl tested 153 URLs successfully; checkout redirects were
  expected because payment is not yet enabled.

## Catalogue search and course-code audit

- Audited all 80 published practice-exam bundles against the current Tertiary
  Courses WSQ/CASL catalogue.
- Added 44 verified TGS course-code aliases covering 42 bundles. The remaining
  38 bundles have no exact verified TGS match and remain searchable by exam
  name, certification code, and vendor.
- Added `TGS-2026061312` for Claude Certified Architect — Foundations after
  verifying its current WSQ page.
- Added seven verified non-TGS course-code aliases, including `C523` for PMP.
- Kept Neo4j `TGS-2023036640` unmapped because no Neo4j practice-exam bundle
  exists; its placement under a training-site category does not make it a
  Tableau certification exam.
- Fixed multi-word vendor searches so `Amazon Web Services` and
  `Linux Foundation` no longer return unrelated vendors.
- Added matching-text/course-code highlighting, a Clear control, improved
  empty-state guidance, and spelling suggestions.
- Confirmed every published bundle remains findable by its title, exam code,
  and vendor. All mapped aliases resolve without duplicate TGS collisions.
- Type checking, ESLint, the production build, database-backed search
  regression tests, and local browser interaction tests passed.
- Restarted the local production server on port 3040 with the updated build;
  `TGS-2026061312` then returned the correct Claude Foundations bundle.

## Resume point

- The application code saved on GitHub is commit `c7b1104` on `main`.
- This refreshed checkpoint note is a subsequent local save and may need to be
  pushed separately if the user wants the note stored on GitHub.
- The updated search is confirmed locally on port 3040. Live deployment of
  `c7b1104` has not yet been verified.
- Coolify still requires an authenticated session before configuring uptime
  monitoring and scheduled PostgreSQL backups.
- Configure monitoring and backups without restarting or disrupting the live
  website. Do not add or expose credentials in source files or chat.
