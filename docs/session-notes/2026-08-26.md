# Session checkpoint — 2026-08-26

## Repository and deployment

- Repository: `https://github.com/alfredang/ai-exams`
- Production: `https://exams.tertiaryinfotech.com`
- Coolify: reachable at `http://72.60.197.117:8000/login`; no authenticated
  dashboard or private build-log access was established.
- Coolify is documented to auto-deploy pushes to `main`.
- Local application was available at `http://127.0.0.1:3040` with the local
  PostgreSQL service reachable on port 5432.

## CySA+ work

- Confirmed CompTIA CySA+ CS0-004 has three published practice exams with 65
  questions each (195 total), plus the published practice/voucher bundle.
- Confirmed all public CompTIA bundle URLs returned HTTP 200.
- Identified CS0-003 as an unused retired V3 catalog shell with zero related
  questions, attempts, orders, entitlements, bundles, reviews, sources, or
  vouchers.
- Removed CS0-003 from the local database using a dependency-count guard.
- Added a non-cascading seed cleanup that deletes CS0-003 only when every
  related-row count is zero; otherwise it preserves the row and warns.
- Commit `af28be8` (`fix(seed): safely remove retired CySA+ CS0-003 shell`) was
  pushed to `main` and deployed through Coolify.

## Security and housekeeping

- Removed known default credentials from current tracked files.
- Changed database seeds and one-shot account utilities so existing password
  hashes are never overwritten. Password environment variables are used only
  to create accounts that do not already exist.
- Changed production helper scripts to require `PROD_ADMIN_PASSWORD` rather
  than falling back to an embedded value.
- Updated examples and Docker Compose to require explicit secret/password
  configuration.
- Removed confirmed ignored/transient audit files, including the local script
  that contained a plaintext production credential. Source PDFs,
  `node_modules`, and `.next` were intentionally retained.
- Commit `1ef07b9` (`fix(security): preserve existing account passwords`) was
  pushed to `main`; GitHub matched the local commit and production continued
  returning HTTP 200 during the Coolify deployment window.

## Verification and constraints

- `npm.cmd run typecheck` passed.
- `npm.cmd run db:seed` passed.
- Existing administrator password fingerprints were identical before and
  after the seed run. Do not reset or rotate user passwords unless the user
  explicitly requests it.
- Production helper scripts passed `node --check` syntax validation.
- Known-default credential scans returned no matches in the current tracked
  tree.
- A full `npm.cmd run build` was blocked by a Windows `EPERM` rename error
  because the running local server held Prisma's engine DLL open. This was a
  local file-lock issue; it was not a TypeScript or seed failure.

## Resume point

- Start from GitHub/main commit `1ef07b9` plus this local checkpoint commit.
- Recheck Git status and GitHub before making new changes.
- Preserve all existing passwords.
- If production deployment details are needed, authenticate to Coolify safely;
  never place credentials in source files or chat.
