# Session checkpoint — 2026-09-02

## Repository status

- Repository: `https://github.com/alfredang/ai-exams`
- Production: `https://exams.tertiaryinfotech.com`
- Branch: `main`
- Latest application commit: `a3c7173` (`fix(search): clear all catalog filters`)
- Local `main` and `origin/main` matched immediately after the push.
- Do not change or reset any existing user passwords.

## Work completed

- Updated the catalogue Clear button to reset the search field, vendor filter,
  and level filter together.
- Clear now returns the URL to `/practice-exams` without query parameters.
- Rebuilt the complete local production application and restarted it on port
  3040.
- Replaced the stale process that had reclaimed port 3040.

## Verification completed

- Confirmed Clear resets vendor to `All vendors` and level to `All levels`.
- Confirmed the search text is emptied and all filter parameters are removed.
- Confirmed `TGS-2026061312` returns Claude Certified Architect — Foundations.
- Type checking and ESLint passed.
- Production build completed successfully.
- Browser interaction test on `http://127.0.0.1:3040/practice-exams` passed
  without console errors.

## Resume point

- Application code through commit `a3c7173` is saved on GitHub.
- The refreshed local site is running on port 3040.
- This checkpoint note is a subsequent local commit and will need a separate
  push if it should also be stored on GitHub.
- Live deployment of `a3c7173` has not yet been verified.
- Payment and voucher-purchase testing remains intentionally out of scope until
  payment functionality is enabled.
